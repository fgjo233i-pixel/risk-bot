# --- مكتبات التداول والاتصال بالمنصات ---
ccxt>=4.0.0             # للاتصال بأكثر من 100 منصة تداول (Binance, Bybit, etc.)

# --- تحليل البيانات المؤشرات الفنية ---
pandas>=2.0.0           # معالجة وتنظيم بيانات الأسعار
numpy>=1.24.0           # العمليات الحسابية السريعة
pandas-ta>=0.3.14b0     # حساب المؤشرات الفنية (بديل متميز لـ TA-Lib لا يتطلب تثبيت C-libraries)

# --- استقبال الإشارات (Webhooks) ---
FastAPI>=0.100.0        # إطار عمل سريع جداً لاستقبال الإشارات (مثل TradingView)
uvicorn>=0.22.0         # خادم لتشغيل تطبيق FastAPI
requests>=2.31.0        # لإرسال واستقبال طلبات HTTP

# --- التنبيهات والإشعارات ---
python-telegram-bot>=20.0 # لإرسال تنبيهات التداول والتقارير عبر تلغرام

# --- إدارة البيئة والإعدادات ---
python-dotenv>=1.0.0    # لقراءة المفاتيح السرية (API Keys) من ملف .env
